#include "main.h"

/**
 * puts2 - print the other char of a str
 * @str: char to be printed
 * Return: 0
 */

void puts2(char *str)
{
	int a, b;

	str[a] = b + 2;
	for (b = 0; b != '\0'; b++)
		a++
}
